const score = document.querySelector('aside > div')
const arrowPattern = [up, down, left, right]

const upArrow = document.querySelector('.up-arrow')
const leftArrow = document.querySelector('.left-arrow')
const downArrow = document.querySelector('down-arrow')
const rightArrow = document.querySelector('.right-arrow')

const changeArrowColor = function() {
    // When up arrow is called, up arrow changes color
    // When down arrow is called, down arrow changes color
    //when left arrow is called, left arrow changes color
    //when right arrow is called right arrow changes color
}
